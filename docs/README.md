# docs

User documentation for vaultspec-core.

The terminal renders and the demo GIF in `assets/` are generated, not hand-drawn. They
are produced by the renderers in `_render/`, which drive real `vaultspec-core` commands
against a throwaway synthetic vault, so edit the renderer rather than the SVG and run
`just docs` to regenerate.
