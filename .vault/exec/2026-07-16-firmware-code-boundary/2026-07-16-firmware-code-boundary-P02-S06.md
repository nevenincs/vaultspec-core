---
tags:
  - '#exec'
  - '#firmware-code-boundary'
date: '2026-07-16'
modified: '2026-07-16'
step_id: 'S06'
related:
  - "[[2026-07-16-firmware-code-boundary-plan]]"
---

# Disambiguate the Traceability requirement in place: mapping lives in the Step Record, never as annotations in code

## Scope

- `src/vaultspec_core/builtins/skills/vaultspec-execute/SKILL.md`

## Description

- Extend the Traceability requirement in
  `src/vaultspec_core/builtins/skills/vaultspec-execute/SKILL.md` to state where the
  change-to-Step mapping lives (the Step Record listing modified files) and where it
  never lives (annotations in the code), naming the boundary and the trailer channel.
- Roll out through install upgrade and sync (source-only edit; the CLI writes the
  `.vaultspec/` mirror).

## Outcome

The only firmware line an executor could previously read as inviting code-side Step
annotations now states the correct direction explicitly. Modified file:
`src/vaultspec_core/builtins/skills/vaultspec-execute/SKILL.md` (mirror regenerated by
the CLI).

## Notes

None.
